{
  "name": "electron",
  "version": "1.0.0",
  "main": "src/main/main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron . --dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:mac": "electron-packager . 'Pixel Music Visualizer' --platform=darwin --arch=x64 --out=dist --icon=src/static/icon.icns --overwrite"
  },
  "author": "Adheena Dev",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "electron": "^35.1.1",
    "electron-packager": "^17.1.2",
    "electron-reloader": "^1.2.3"
  }
}
